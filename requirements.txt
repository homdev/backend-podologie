Flask==2.1.3                 # Mise à jour pour rester compatible avec Python 3.10+
Flask-Cors==3.0.10           # Version compatible avec Flask 2.1.3
opencv-python==4.5.5.64      # Version récente stable avec support CPU
matplotlib==3.5.2            # Compatible avec Python 3.10 et numpy récent
Werkzeug==2.1.3              # Mise à jour pour correspondre à Flask
numpy==1.22.0                # Compatible avec les versions récentes de torch
pillow==9.2.0                # Nouvelle version pour éviter d’éventuels conflits
torch==2.0.1+cpu             # Version CPU stable pour éviter les erreurs de compilation
torchvision==0.15.2+cpu      # Compatible avec torch 2.0.1+cpu
gunicorn==20.1.0             # Pas de mise à jour nécessaire, stable
python-dotenv==0.21.0        # Mise à jour pour rester compatible avec Python 3.10+
transformers==4.30.0         # Version récente, stable
gdown==4.7.1                 # Dernière version compatible
