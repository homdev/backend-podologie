[phases.setup]
nixPkgs = ['python39', 'python39Packages.pip', 'gcc', 'pkg-config']

[phases.install]
cmds = [
    'python -m pip install --break-system-packages -r requirements.txt'
]

[start]
cmd = 'gunicorn app:app --bind=0.0.0.0:$PORT'
